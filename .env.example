# Cloudflare Tunnel token (required for cloudflared service)
# TUNNEL_TOKEN=your-tunnel-token

# ttyd basic authentication credentials in the form "username:password".
# Strongly recommended when exposing ttyd remotely (for example via Cloudflare Tunnel).
# TTYD_CREDENTIAL=your-username:your-strong-password
